<!--run diesen command um server zu starten:
//cd say-my-name
ng serve --open

wenn nicht startet diesen code benutzen:

# Clear npm cache
npm cache clean --force

# Delete node_modules and lock files
rm -rf node_modules package-lock.json .angular/cache

# Fresh install
npm install

(einfach komplett copy paste und einfügen und dann wider ng serve --open und sollte funktionieren)


Server stoppen: 
        im Terminal:
        STRG + C -> ggf. mit Y bestätigen

Alles schön formatieren mit 
        ALT + SHIFT + F
        oder nur Auswahl formatieren:   STRG + K DANN!! STRG + F          

-->